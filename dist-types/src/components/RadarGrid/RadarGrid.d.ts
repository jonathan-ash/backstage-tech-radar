/// <reference types="react" />
import type { Ring } from '../../utils/types';
export declare type Props = {
    radius: number;
    rings: Ring[];
};
declare const RadarGrid: (props: Props) => JSX.Element;
export default RadarGrid;
